# Copy to .env and fill - do NOT commit real secrets to git
# Example placeholders — copy this file to `.env` and replace values
DISCORD_TOKEN=REPLACE_WITH_YOUR_DISCORD_BOT_TOKEN

# Optional: limit command registration to a specific guild during development
GUILD_ID=REPLACE_WITH_GUILD_ID_OPTIONAL

# Optional: if set, /ask will only work in this channel
CHANNEL_ID=REPLACE_WITH_CHANNEL_ID_OPTIONAL
LLM_URL=https://your-llm-endpoint.example/predict
LLM_API_KEY=REPLACE_WITH_API_KEY_OR_LEAVE_EMPTY

# Optional: local CSV fallback path (used when csv_url not provided)
CSV_PATH=./csvs/data.csv

# Optional: maximum number of CSV rows to include in LLM payload
MAX_CSV_ROWS=

USE_SYSTEM_DYNAMICS_BOT=true
DIAGRAM_EXPORTER=graphviz